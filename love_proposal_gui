import tkinter as tk
from tkinter import messagebox

def say_yes():
    messagebox.showinfo(
        "💍 She Said YES 💍",
        "You just made me the happiest person ❤️\nI love you forever 🥰"
    )

def say_no():
    messagebox.showwarning(
        "💔",
        "Error 404: Love Not Found 😢\nBut my love will never stop ❤️"
    )

root = tk.Tk()
root.title("💖 My Python Proposal 💖")
root.geometry("400x300")
root.configure(bg="pink")

label = tk.Label(
    root,
    text="Hey My Love ❤️\nWill you be my girlfriend? 💍",
    font=("Arial", 16, "bold"),
    bg="pink",
    fg="red"
)
label.pack(pady=30)

yes_btn = tk.Button(
    root,
    text="YES ❤️",
    font=("Arial", 14, "bold"),
    bg="red",
    fg="white",
    command=say_yes
)
yes_btn.pack(side="left", padx=40, pady=20)

no_btn = tk.Button(
    root,
    text="NO 💔",
    font=("Arial", 14, "bold"),
    bg="white",
    fg="black",
    command=say_no
)
no_btn.pack(side="right", padx=40, pady=20)

root.mainloop()

